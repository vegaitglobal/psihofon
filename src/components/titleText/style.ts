import {StyleSheet, TextStyle} from 'react-native';

export default StyleSheet.create({
  titleText: {
    fontSize: 24,
    lineHeight: 36,
    fontWeight: '600',
    fontStyle: 'normal',
  } as TextStyle,
});
